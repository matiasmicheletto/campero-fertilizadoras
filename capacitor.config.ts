import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.inta.camperofertilizadoras',
  appName: 'Campero Fertilizadoras',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
